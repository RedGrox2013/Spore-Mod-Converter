<Window x:Class="Spore_Mod_Converter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Spore_Mod_Converter"
        mc:Ignorable="d"
        Title="Spore Mod Converter by RedGrox"
        WindowStartupLocation="CenterScreen"
        Height="450" Width="600" ResizeMode="CanMinimize">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Image HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Source="/Images/icon.png"
               Height="300" Width="300"/>

        <Grid Grid.Row="1" Height="25" Margin="5" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            
            <TextBox ToolTip="Package file path"
                     VerticalContentAlignment="Center"
                     Margin="0 0 3 0"
                     IsEnabled="{Binding Enable}"
                     Text="{Binding PackagePath}"/>
            <Button Grid.Column="1" IsEnabled="{Binding Enable}" Command="{Binding BrowseFileCommand}">Browse</Button>
        </Grid>

        <DockPanel Grid.Row="2" Height="50" HorizontalAlignment="Right" IsEnabled="{Binding Enable}" LastChildFill="False">
            <Button DockPanel.Dock="Left" Margin="5" Background="GreenYellow" Command="{Binding ConvertCommand}">Convert</Button>
            <Button DockPanel.Dock="Right" Margin="5" Command="{Binding OpenOutputDirectoryCommand}">Open output folder</Button>
        </DockPanel>
    </Grid>
</Window>
